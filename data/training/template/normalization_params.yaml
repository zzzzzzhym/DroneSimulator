# manual normalization parameters. Omitted fields will default to no normalization (mean = 0, scale = 1)
input:
  rotor_0_rotation_spd:
    mean: 1000.0
    scale: 0.01
  rotor_1_rotation_spd:
    mean: 1000.0
    scale: 0.01
  rotor_2_rotation_spd:
    mean: 1000.0
    scale: 0.01
  rotor_3_rotation_spd:
    mean: 1000.0
    scale: 0.01
label:

