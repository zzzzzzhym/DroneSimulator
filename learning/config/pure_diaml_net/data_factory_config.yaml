a_shot: 128    # batch size for solving optimal adaptation coeff
phi_shot: 256    # batch size of phi net training
can_shuffle: True  # whether to shuffle the data in training
data_usage_ratio: 1.0  # ratio of data used in training, 1.0 means all data is used